<UserControl x:Class="Caros.Pages.InputPageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:metro="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:laurencebunnage="clr-namespace:TilePanel;assembly=TilePanel"
             xmlns:cal="http://www.caliburnproject.org"
             mc:Ignorable="d" 
             Width="1024" Height="600">
    <Grid VerticalAlignment="Center" >
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding Message, FallbackValue='Enter value'}" FontSize="36" TextWrapping="Wrap"/>
        <TextBox    Grid.Row="1" Text="{Binding Value, Mode=TwoWay}" FontSize="36"/>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Height="100">
            <Button x:Name="Accept" Content="{Binding OkButtonText, FallbackValue='Accept'}" Width="300" Margin="20,5"/>
            <Button x:Name="Cancel" Content="{Binding CancelButtonText, FallbackValue='Cancel'}" Width="300" Margin="20,5"/>
        </StackPanel>
    </Grid>
</UserControl>
